<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property name="deploymentEnvironment" value="${DEPLOYMENT_ENVIRONMENT:-unknown}"/>
  <property name="serviceVersion" value="${SERVICE_VERSION:-unknown}"/>

  <springProfile name="!local">
    <!-- Production: structured JSON logging for Grafana Loki ingestion -->
    <appender name="JSON_STDOUT" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <!-- OTel Java agent's logback-mdc instrumentation injects
             these snake_case keys into MDC for every log call inside a
             traced request. The historical Sleuth convention was
             camelCase (traceId/spanId); the agent uses OTel-standard
             snake_case (trace_id/span_id/trace_flags). The encoder
             surfaces them as top-level JSON fields, which Loki's
             derivedFields can then turn into clickable trace links. -->
        <includeMdcKeyName>trace_id</includeMdcKeyName>
        <includeMdcKeyName>span_id</includeMdcKeyName>
        <includeMdcKeyName>trace_flags</includeMdcKeyName>
        <customFields>{"service":"auth-api","service.name":"auth-api","service.version":"${serviceVersion}","deployment.environment":"${deploymentEnvironment}"}</customFields>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="JSON_STDOUT"/>
    </root>
  </springProfile>

  <springProfile name="local">
    <!-- Local development: human-readable logging -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>
    <root level="DEBUG">
      <appender-ref ref="STDOUT"/>
    </root>
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.jooq" level="DEBUG"/>
  </springProfile>
</configuration>
